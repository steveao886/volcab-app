# Volcab v1.1 Wave 1 (A Review Tweaks / F New Question Types / D Learning Data) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship three items from the v1.1 design doc: A (review-flow tweaks), F (three new quiz question types), D (learning-data visualization); B/C/E are covered in a separate plan.

**Architecture:** The three items don't overlap and can be worked in three parallel tracks. All new real logic lives in pure-function files with mandatory TDD; no component tests for UI itself (following the same decision established in the v1.0 plan: logic is verified at the pure-function layer, the UI is covered by behavior contracts + manual verification). No new npm dependencies, no charting library.

**Tech Stack:** Vite 8 / React 19 / TypeScript / react-router-dom 7 (HashRouter) / Vitest + happy-dom.

**Spec:** `docs/superpowers/specs/2026-07-25-volcab-v1.1-design.md` (§2 = A, §7 = F, §5 = D; the spec is authoritative)

**Baseline:** `master`, all 174 tests passing, `npx tsc -b --noEmit` / `npm run build` / `npx oxlint` / `npm run validate-words` all green.

---

## Parallelism and File Ownership

The three items' file sets don't intersect, so three worktrees can run in parallel. **Each task group may only touch its own column**:

| | A | F | D |
|---|---|---|---|
| | `src/pages/Review.tsx` | `src/lib/quiz.ts` | `src/pages/Today.tsx` |
| | `src/pages/Review.css` | `src/lib/quiz.test.ts` | `src/pages/Today.css` |
| | `src/pages/reviewQueue.ts` | `src/pages/Quiz.tsx` | `src/pages/statsDerive.ts` (new) |
| | `src/pages/reviewQueue.test.ts` | `src/pages/QuizQuestion.tsx` | `src/pages/statsDerive.test.ts` (new) |
| | | `src/pages/Quiz.css` | `src/pages/Stats.tsx` (new) |
| | | | `src/pages/Stats.css` (new) |
| | | | `src/App.tsx` (only adding one route) |

**Frozen** (no group may touch these): `src/types.ts`, `src/state/*`, `src/lib/` (except `quiz.ts`), `src/components/*`, `src/styles/*`, `index.html`, `vite.config.ts`, `package.json`. If a component variant is missing, write it into that group's own page CSS and list it in the report.

---

## Phase A — Review-flow tweaks

### Task 1: Cards remaining

**Files:**
- Modify: `src/pages/reviewQueue.ts`
- Test: `src/pages/reviewQueue.test.ts`
- Modify: `src/pages/Review.tsx:205-222`

- [ ] **Step 1: Write failing tests**

Append to `src/pages/reviewQueue.test.ts`:

```ts
describe('remaining', () => {
  it('等于队列里还没打分的卡数', () => {
    const q = buildSessionQueue(['a', 'b'], ['c'])
    expect(remaining(q)).toBe(3)
  })
  it('打一张分就少一张', () => {
    const q = advance(buildSessionQueue(['a'], ['b']), 'a', undefined, '2026-07-25')
    expect(remaining(q)).toBe(1)
  })
  it('学习步长重现:总数不变,剩余不减 —— 只是下降变慢而不是变多', () => {
    const q0 = buildSessionQueue([], ['a', 'b'])
    const learning: ProgressEntry = {
      state: 'learning', ease: 2.5, intervalDays: 0, due: '2026-07-25',
      stepIndex: 1, reps: 1, lapses: 0, lastReviewedAt: '2026-07-25T00:00:00Z',
    }
    const q1 = advance(q0, 'a', learning, '2026-07-25')
    expect(remaining(q0)).toBe(2)
    expect(remaining(q1)).toBe(2) // a 被塞回队尾:还是两张要看
  })
  it('清空后为 0', () => {
    const q = advance(buildSessionQueue([], ['a']), 'a', undefined, '2026-07-25')
    expect(remaining(q)).toBe(0)
    expect(isDone(q)).toBe(true)
  })
  it('dropCurrent 让剩余减一', () => {
    const q = dropCurrent(buildSessionQueue(['a', 'b'], []))
    expect(remaining(q)).toBe(1)
  })
})
```

Add `remaining` to the top import (on the same line as `dropCurrent`).

- [ ] **Step 2: Run and confirm it fails**

Run: `npx vitest run src/pages/reviewQueue.test.ts`
Expected: FAIL, `remaining is not a function`

- [ ] **Step 3: Implement**

Append to `src/pages/reviewQueue.ts`:

```ts
/**
 * How many cards are still left to see.
 *
 * Why not just display seen/total directly: total grows every time a card
 * reappears via the learning-step cycle, so a user who set "50 new words a
 * day" would see a denominator of 60, and their first reaction is that
 * something's miscounted. Cards-remaining only ever decreases, so it never
 * conflicts with any setting; a reappearance just slows the decrease.
 */
export function remaining(q: SessionQueue): number {
  return q.ids.length
}
```

- [ ] **Step 4: Run and confirm it passes**

Run: `npx vitest run src/pages/reviewQueue.test.ts` → all PASS

- [ ] **Step 5: Update the rendering**

In `src/pages/Review.tsx`, replace the entire progress block with:

```tsx
      <div className="review-progress">
        <div
          className="progress"
          role="progressbar"
          aria-valuemin={0}
          aria-valuemax={queue.total}
          aria-valuenow={queue.seen}
          aria-valuetext={`还剩 ${remaining(queue)} 张`}
        >
          <div className="progress__fill" style={{ width: `${(queue.seen / queue.total) * 100}%` }} />
        </div>
        <p className="num muted review-progress__count">还剩 {remaining(queue)} 张</p>
      </div>
```

Add `remaining` to the top import. **The progress bar itself keeps computing off seen/total** — it expresses "how far through this round you are," while cards-remaining expresses "how many are left to see" — each does its own job.

- [ ] **Step 6: Verify and commit**

Run: `npx tsc -b --noEmit && npm test && npx oxlint`
Expected: all green, test count goes from 174 to 179

```bash
git add src/pages/reviewQueue.ts src/pages/reviewQueue.test.ts src/pages/Review.tsx
git commit -m "feat: 复习进度改为显示剩余张数"
```

### Task 2: Delete an entry from the review card

**Files:**
- Modify: `src/pages/Review.tsx`, `src/pages/Review.css`

No new pure logic, no tests added here (the queue-side `dropCurrent` is already covered).

- [ ] **Step 1: Wire up store's deleteWords and the confirmation dialog**

At the top of `Review.tsx`: add `deleteWords` to the `useApp()` destructure; `import { ConfirmDialog } from '../components/ConfirmDialog'`; add two new pieces of state:

```tsx
const [confirmDelete, setConfirmDelete] = useState(false)
const [deleting, setDeleting] = useState(false)
```

- [ ] **Step 2: Delete handler**

```tsx
  function handleDelete() {
    if (curWord === undefined || deleting) return
    setDeleting(true)
    // 与 WordDetail 同一套取舍:本地删除已是权威结果,不等网络推送完成。
    // 这里额外要把它从会话队列里摘掉,否则队首还指着一个已不存在的词。
    void deleteWords([curWord.id]).finally(() => setDeleting(false))
    setQueue(q => dropCurrent(q))
    setConfirmDelete(false)
  }
```

Add `dropCurrent` to the import.

- [ ] **Step 3: Render the delete entry point (only after flipping)**

Append this, **after** the `.review-actions` block that holds the four grading buttons, still inside the card area:

```tsx
      {flipped && (
        <div className="review-cull">
          <Button variant="ghost" size="sm" onClick={() => setConfirmDelete(true)}>
            删除这个词
          </Button>
        </div>
      )}
```

**Must be gated on `flipped &&`**: before seeing the definition there's no way to judge whether the word should be kept, and the front only has two big targets, headword and pronunciation — adding an irreversible button there would make mis-taps too costly.

- [ ] **Step 4: Confirmation dialog**

Append this at the very end, inside `<Page>`:

```tsx
      <ConfirmDialog
        open={confirmDelete}
        titleId="review-delete-title"
        title={`删除「${curWord?.headword ?? ''}」?`}
        body="这个词条以及它的学习进度(状态、复习次数、失误次数等)会一并删除,且无法恢复。"
        confirmLabel="确认删除"
        busy={deleting}
        onConfirm={handleDelete}
        onCancel={() => setConfirmDelete(false)}
      />
```

Wording matches the entry detail page word-for-word — this is the only data-destroying action in the whole app, and it shouldn't have two different phrasings.

- [ ] **Step 5: Styling**

Append to `src/pages/Review.css`:

```css
/* 剔除入口:与四档打分保持距离,视觉上明确不属于同一组操作 */
.review-cull {
  display: flex;
  justify-content: center;
  margin-top: var(--sp-4);
  padding-top: var(--sp-3);
  border-top: 1px solid var(--rule);
}
```

- [ ] **Step 6: Verify and commit**

Run: `npx tsc -b --noEmit && npm test && npm run build && npx oxlint` → all green (test count unchanged)

Manual contract (the browser can't take screenshots here, verify with DOM assertions instead): no `.review-cull` before flipping; present after flipping; clicking it brings up `dialog[open]`; canceling has no side effect; after confirming, the word disappears from `localStorage['volcab.words']`, that id is cleared from `volcab.progress`, cards-remaining decreases by one, the UI stays on the next card.

```bash
git add src/pages/Review.tsx src/pages/Review.css
git commit -m "feat: 复习卡翻面后可删除该词条"
```

---

## Phase F — New quiz question types

The existing implementation is in `src/lib/quiz.ts`: `generateQuiz` uses a deterministic rotation via `types[questions.length % types.length]`, distractors are collected deduped by `pickDistractorLabels`, and a candidate word is skipped if 3 distractors can't be assembled. **The new question types must follow both of these existing conventions.**

### Task 3: Example-sentence cloze

**Files:**
- Modify: `src/lib/quiz.ts`
- Test: `src/lib/quiz.test.ts`

- [ ] **Step 1: Write failing tests**

Append to `src/lib/quiz.test.ts`:

```ts
describe('clozeExample', () => {
  it('挖掉原形出现的词头', () => {
    expect(clozeExample('She concoct a story quickly.', 'concoct'))
      .toBe('She ___ a story quickly.')
  })
  it('挖掉变形出现的词头', () => {
    expect(clozeExample('She concocted an elaborate excuse.', 'concoct'))
      .toBe('She ___ an elaborate excuse.')
  })
  it('大小写不敏感', () => {
    expect(clozeExample('Concocting excuses is his talent.', 'concoct'))
      .toBe('___ excuses is his talent.')
  })
  it('同句多次出现时全部挖掉,不留下泄题的那一处', () => {
    expect(clozeExample('He concocted it, then concocted more.', 'concoct'))
      .toBe('He ___ it, then ___ more.')
  })
  it('多词词头按整体挖', () => {
    expect(clozeExample('They agreed on an ad hoc basis.', 'ad hoc'))
      .toBe('They agreed on an ___ basis.')
  })
  it('定位不到就返回 null,由调用方跳过该例句', () => {
    expect(clozeExample('Nothing relevant here.', 'concoct')).toBeNull()
  })
})
```

- [ ] **Step 2: Run and confirm it fails**

Run: `npx vitest run src/lib/quiz.test.ts` → FAIL, `clozeExample is not a function`

- [ ] **Step 3: Implement**

Append to `src/lib/quiz.ts` (right after `meaningLabel`):

```ts
const BLANK = '___'

const escapeRe = (s: string) => s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')

/**
 * 把例句里的词头挖成空格。
 *
 * 实测 476 词中 86% 的例句含词头原形,14% 只含变形(concocted / concocting),
 * 0% 完全定位不到 —— 所以必须同时处理变形,只做全词匹配会漏掉 68 个词。
 * 做法:取词干(去掉尾部至多 3 个字母)后允许跟任意字母后缀。
 *
 * 同句多次出现全部挖掉:留下任何一处都会直接泄题。
 * 定位不到返回 null —— 宁可跳过这条例句,也不出一道没有空格的挖空题。
 */
export function clozeExample(sentence: string, headword: string): string | null {
  const h = headword.trim().toLowerCase()
  const exact = new RegExp(`\\b${escapeRe(h)}\\b`, 'gi')
  if (exact.test(sentence)) return sentence.replace(exact, BLANK)

  // 变形:词干 + 任意字母后缀。短词不截断,避免 "act" 命中 "action" 一类误伤。
  const stem = h.length > 5 ? h.slice(0, h.length - 3) : h
  const inflected = new RegExp(`\\b${escapeRe(stem)}[a-z]*\\b`, 'gi')
  if (inflected.test(sentence)) return sentence.replace(inflected, BLANK)

  return null
}
```

- [ ] **Step 4: Run and confirm it passes** → `npx vitest run src/lib/quiz.test.ts` all PASS

- [ ] **Step 5: Commit**

```bash
git add src/lib/quiz.ts src/lib/quiz.test.ts
git commit -m "feat: 例句挖空的纯函数"
```

### Task 4: Pure functions for collocation fill-in and synonym/antonym hints

**Files:**
- Modify: `src/lib/quiz.ts`
- Test: `src/lib/quiz.test.ts`

- [ ] **Step 1: Write failing tests**

```ts
describe('clozeCollocation', () => {
  it('挖掉搭配里的词头', () => {
    expect(clozeCollocation('abrogate a treaty', 'abrogate')).toBe('___ a treaty')
  })
  it('词头在中间也能挖', () => {
    expect(clozeCollocation('formally abrogate an accord', 'abrogate'))
      .toBe('formally ___ an accord')
  })
  it('变形同样处理', () => {
    expect(clozeCollocation('abrogated the agreement', 'abrogate'))
      .toBe('___ the agreement')
  })
  it('定位不到返回 null', () => {
    expect(clozeCollocation('a binding accord', 'abrogate')).toBeNull()
  })
})

describe('sharedSynonyms', () => {
  it('找出被多个词条共享的同义/反义词(小写归一)', () => {
    const ws = [
      word('alpha', '甲'), word('bravo', '乙'),
    ]
    ws[0].synonyms = ['Common', 'onlyA']
    ws[1].synonyms = ['common', 'onlyB']
    const shared = sharedSynonyms(ws)
    expect(shared.has('common')).toBe(true)
    expect(shared.has('onlya')).toBe(false)
  })
  it('反义词与同义词一起统计', () => {
    const ws = [word('alpha', '甲'), word('bravo', '乙')]
    ws[0].synonyms = ['x']
    ws[1].antonyms = ['X']
    expect(sharedSynonyms(ws).has('x')).toBe(true)
  })
})
```

(`word(id, zh)` is the existing construction helper already in this test file, reused directly.)

- [ ] **Step 2: Run and confirm it fails** → FAIL

- [ ] **Step 3: Implement**

```ts
/** 搭配挖空。规则与 clozeExample 相同,单列一个函数是因为搭配是短语、语义不同。 */
export function clozeCollocation(collocation: string, headword: string): string | null {
  return clozeExample(collocation, headword)
}

/**
 * 被一个以上词条共享的近义/反义词(全部小写)。
 *
 * 实测 1597 个同义词里有 228 个出现在多个词条(overbearing、decree、flexibility……)。
 * 拿它们当提示会出现「两个选项都对」,用户会判定测验有缺陷 —— 所以出题时必须排除。
 */
export function sharedSynonyms(words: Word[]): Set<string> {
  const count = new Map<string, number>()
  for (const w of words) {
    for (const s of [...w.synonyms, ...w.antonyms]) {
      const k = s.trim().toLowerCase()
      count.set(k, (count.get(k) ?? 0) + 1)
    }
  }
  return new Set([...count.entries()].filter(([, c]) => c > 1).map(([k]) => k))
}
```

- [ ] **Step 4: Run and confirm it passes** → all PASS

- [ ] **Step 5: Commit**

```bash
git add src/lib/quiz.ts src/lib/quiz.test.ts
git commit -m "feat: 搭配挖空与共享近义词识别"
```

### Task 5: Wire the three new question types into the generator

**Files:**
- Modify: `src/lib/quiz.ts`
- Test: `src/lib/quiz.test.ts`

- [ ] **Step 1: Extend the type and update the existing assertion**

Change `QuizType` to:

```ts
export type QuizType =
  | 'word2meaning' | 'meaning2word' | 'spelling'
  | 'clozeExample' | 'clozeCollocation' | 'synonymHint'
```

Add one field to `QuizQuestion`:

```ts
  /** 仅 synonymHint 题携带:提示词是近义还是反义,界面必须标明,
   *  否则用户无从判断该选意思相同的还是相反的。 */
  hintKind?: 'synonym' | 'antonym'
```

**Update the existing assertion** (`src/lib/quiz.test.ts:31`). This is the first time this project modifies an existing test's assertion, and it must express the actual intent rather than just swapping in a different hardcoded number:

```ts
    // 题型确定性轮换,一轮内均衡分布。这里不再硬编码题型总数 ——
    // 断言的是「轮换」这个契约本身,新增题型时不必再改这一行。
    const types = qs.map(q => q.type)
    expect(new Set(types).size).toBe(Math.min(qs.length, QUIZ_TYPES.length))
```

And export `QUIZ_TYPES` from `quiz.ts`:

```ts
export const QUIZ_TYPES: readonly QuizType[] = [
  'word2meaning', 'meaning2word', 'spelling',
  'clozeExample', 'clozeCollocation', 'synonymHint',
]
```

- [ ] **Step 2: Write failing tests for the new question types**

```ts
describe('新题型', () => {
  it('例句挖空:提示含空格且不含答案词,四个词头选一', () => {
    const qs = generateQuiz(words, studied(), 12, seq())
    const q = qs.find(x => x.type === 'clozeExample')
    if (q === undefined) return // 该轮未轮到,不算失败
    expect(q.prompt).toContain('___')
    expect(q.prompt.toLowerCase()).not.toContain(q.answer.toLowerCase())
    expect(q.options).toHaveLength(4)
    expect(q.options).toContain(q.answer)
  })
  it('搭配填空:同样不泄题', () => {
    const qs = generateQuiz(words, studied(), 12, seq())
    const q = qs.find(x => x.type === 'clozeCollocation')
    if (q === undefined) return
    expect(q.prompt).toContain('___')
    expect(q.prompt.toLowerCase()).not.toContain(q.answer.toLowerCase())
  })
  it('近义/反义提示:标明种类,且提示词不是共享词', () => {
    const qs = generateQuiz(words, studied(), 12, seq())
    const q = qs.find(x => x.type === 'synonymHint')
    if (q === undefined) return
    expect(q.hintKind === 'synonym' || q.hintKind === 'antonym').toBe(true)
    expect(sharedSynonyms(words).has(q.prompt.toLowerCase())).toBe(false)
  })
})
```

- [ ] **Step 3: Run and confirm it fails** → not a single one of the new question types can be generated

- [ ] **Step 4: Implement the generation branches**

In `generateQuiz`'s loop, insert three branches after the `spelling` branch and before `labelFn`. All three use the headword as the option, so they share one set of distractor-collection logic:

```ts
    const headwordLabel = (x: Word) => x.headword

    if (type === 'clozeExample' || type === 'clozeCollocation') {
      const sources = type === 'clozeExample' ? w.examples : w.collocations
      let prompt: string | null = null
      for (const s of sources) {
        prompt = clozeExample(s, w.headword)
        if (prompt !== null) break
      }
      if (prompt === null) continue // 这条词的例句/搭配都定位不到词头,换下一个候选词
      const distractors = pickDistractorLabels(w, w.headword, headwordLabel, pool, words, rng)
      if (!distractors) continue
      questions.push({
        type, wordId: w.id, prompt,
        options: shuffle([w.headword, ...distractors], rng),
        answer: w.headword,
      })
      continue
    }

    if (type === 'synonymHint') {
      const shared = sharedSynonymsCache
      const syn = w.synonyms.find(s => !shared.has(s.trim().toLowerCase()))
      const ant = w.antonyms.find(s => !shared.has(s.trim().toLowerCase()))
      const hint = syn ?? ant
      if (hint === undefined) continue // 该词的近反义词全被共享,换下一个候选词
      const distractors = pickDistractorLabels(w, w.headword, headwordLabel, pool, words, rng)
      if (!distractors) continue
      questions.push({
        type, wordId: w.id, prompt: hint,
        options: shuffle([w.headword, ...distractors], rng),
        answer: w.headword,
        hintKind: syn !== undefined ? 'synonym' : 'antonym',
      })
      continue
    }
```

Compute this once at the top of `generateQuiz` (after `pool`):

```ts
  // 共享词集合对全词库只算一次 —— 放进循环会变成 O(n²)
  const sharedSynonymsCache = sharedSynonyms(words)
```

And change `const types: QuizType[] = [...]` to `const types = QUIZ_TYPES`.

- [ ] **Step 5: Run and confirm it passes**

Run: `npm test`
Expected: all green. The existing quiz tests (other than the one deliberately changed) pass unmodified.

- [ ] **Step 6: Commit**

```bash
git add src/lib/quiz.ts src/lib/quiz.test.ts
git commit -m "feat: 测验新增例句挖空/搭配填空/近义反义提示三种题型"
```

### Task 6: Render the new question types on the quiz page

**Files:**
- Modify: `src/pages/QuizQuestion.tsx`, `src/pages/Quiz.css`

- [ ] **Step 1: Render branches**

All three new question types are "a prompt + pick from four headwords," with interaction identical to the existing `meaning2word`, so they **reuse the same options-rendering code**, only the prompt area differs:

- `clozeExample` / `clozeCollocation`: the prompt is a full sentence, wrapped in `lang="en"`, with `.quiz-blank` styling on the `___` to make the blank stand out.
- `synonymHint`: a small label above the prompt word — `hintKind === 'synonym'` shows "Which word is closest in meaning?", `'antonym'` shows "Which word means the opposite?" **This label can't be dropped** — without it the user has no way to know whether to pick the similar word or the opposite one.

- [ ] **Step 2: Styling**

Append to `src/pages/Quiz.css`:

```css
/* 挖空处:比正文重,一眼能定位到要填的地方 */
.quiz-blank {
  font-weight: 700;
  letter-spacing: 0.08em;
  color: var(--text);
}

/* 近义/反义提示的种类标签 */
.quiz-hint-kind {
  margin-bottom: var(--sp-2);
}
```

The kind label reuses the existing `.section-title` from `components.css`, no new style invented.

- [ ] **Step 3: Verify and commit**

Run: `npx tsc -b --noEmit && npm test && npm run build && npx oxlint` → all green

Manual contract: all six question types show up in a balanced way across a 10-question round; cloze questions show `___` in the prompt without the answer; synonym/antonym questions show the kind label; long example sentences don't overflow horizontally at 375px (the cloze prompt is a full English sentence, the longest text on this page).

```bash
git add src/pages/QuizQuestion.tsx src/pages/Quiz.css
git commit -m "feat: 测验页渲染三种新题型"
```

---

## Phase D — Learning-data visualization

### Task 7: Stats-derivation pure functions

**Files:**
- Create: `src/pages/statsDerive.ts`, `src/pages/statsDerive.test.ts`

Every metric can only be derived from `progress.dailyStats` and each word's `state` — `progress.json`'s structure isn't changed (reasoning in spec §5.1: a full log would hit GitHub's 1 MB read limit in about 9 months).

- [ ] **Step 1: Write failing tests**

```ts
import { describe, expect, it } from 'vitest'
import { emptyProgress } from '../types'
import type { Progress, Word } from '../types'
import { dailySeries, accuracySeries, masteryBreakdown, cumulativeTotals } from './statsDerive'

const prog = (stats: Progress['dailyStats']): Progress => ({ ...emptyProgress(), dailyStats: stats })

describe('dailySeries', () => {
  it('按日期连续补齐,缺失的日子记 0', () => {
    const s = dailySeries(prog({ '2026-07-25': { reviewed: 5, newLearned: 2, correct: 4, quizTaken: 0 } }), '2026-07-25', 3)
    expect(s.map(d => d.date)).toEqual(['2026-07-23', '2026-07-24', '2026-07-25'])
    expect(s.map(d => d.reviewed)).toEqual([0, 0, 5])
  })
  it('空进度返回全 0 而不是空数组', () => {
    const s = dailySeries(emptyProgress(), '2026-07-25', 2)
    expect(s).toHaveLength(2)
    expect(s.every(d => d.reviewed === 0)).toBe(true)
  })
})

describe('accuracySeries', () => {
  it('正确率 = correct / reviewed', () => {
    const s = accuracySeries(prog({ '2026-07-25': { reviewed: 10, newLearned: 0, correct: 8, quizTaken: 0 } }), '2026-07-25', 1)
    expect(s[0].accuracy).toBeCloseTo(0.8)
  })
  it('当天没复习时正确率为 null,不是 0 也不是 NaN', () => {
    const s = accuracySeries(emptyProgress(), '2026-07-25', 1)
    expect(s[0].accuracy).toBeNull()
  })
})

describe('masteryBreakdown', () => {
  it('未学/学习中/已掌握三档计数,无记录视为未学', () => {
    const words = [w('a'), w('b'), w('c')]
    const p = emptyProgress()
    p.words['a'] = entry('learning')
    p.words['b'] = entry('review')
    expect(masteryBreakdown(words, p)).toEqual({ new: 1, learning: 1, review: 1, total: 3 })
  })
  it('空词库不产生除零', () => {
    expect(masteryBreakdown([], emptyProgress())).toEqual({ new: 0, learning: 0, review: 0, total: 0 })
  })
})

describe('cumulativeTotals', () => {
  it('累计复习次数与平均每日新词', () => {
    const p = prog({
      '2026-07-24': { reviewed: 10, newLearned: 4, correct: 9, quizTaken: 1 },
      '2026-07-25': { reviewed: 6, newLearned: 2, correct: 5, quizTaken: 0 },
    })
    const t = cumulativeTotals(p)
    expect(t.totalReviewed).toBe(16)
    expect(t.activeDays).toBe(2)
    expect(t.avgNewPerActiveDay).toBeCloseTo(3)
  })
  it('无数据时平均值为 0 而不是 NaN', () => {
    expect(cumulativeTotals(emptyProgress()).avgNewPerActiveDay).toBe(0)
  })
})
```

Add the test construction helper at the top (same pattern as `libraryFilter.test.ts`):

```ts
const w = (id: string): Word => ({
  id, headword: id, phonetic: '/x/', meanings: [{ pos: 'v.', en: 'x', zh: 'x' }],
  examples: ['a', 'b'], synonyms: [], antonyms: [], collocations: [],
  relatedForms: [], sourceNote: 'manual', addedAt: '2026-07-01',
})
const entry = (state: 'learning' | 'review') => ({
  state, ease: 2.5, intervalDays: 1, due: '2026-07-25',
  stepIndex: 0, reps: 1, lapses: 0, lastReviewedAt: '2026-07-25T00:00:00Z',
})
```

- [ ] **Step 2: Run and confirm it fails**

Run: `npx vitest run src/pages/statsDerive.test.ts`
Expected: FAIL, `Failed to resolve import "./statsDerive"`

- [ ] **Step 3: Implement**

```ts
import { addDays } from '../lib/srs'
import type { Progress, Word } from '../types'

export interface DayPoint { date: string; reviewed: number; newLearned: number }
export interface AccuracyPoint { date: string; accuracy: number | null }
export interface Mastery { new: number; learning: number; review: number; total: number }
export interface Totals { totalReviewed: number; activeDays: number; avgNewPerActiveDay: number }

/** 从 today 往回数 days 天的连续序列;缺失的日子补 0,图表才不会有断口。 */
export function dailySeries(progress: Progress, today: string, days: number): DayPoint[] {
  const out: DayPoint[] = []
  for (let i = days - 1; i >= 0; i--) {
    const date = addDays(today, -i)
    const s = progress.dailyStats[date]
    out.push({ date, reviewed: s?.reviewed ?? 0, newLearned: s?.newLearned ?? 0 })
  }
  return out
}

/** 正确率。当天没复习时为 null —— 0% 会让折线掉到底,谎称「那天全错了」。 */
export function accuracySeries(progress: Progress, today: string, days: number): AccuracyPoint[] {
  const out: AccuracyPoint[] = []
  for (let i = days - 1; i >= 0; i--) {
    const date = addDays(today, -i)
    const s = progress.dailyStats[date]
    out.push({ date, accuracy: s && s.reviewed > 0 ? s.correct / s.reviewed : null })
  }
  return out
}

/** 词库掌握分布。progress 里没有记录的词视为未学。 */
export function masteryBreakdown(words: Word[], progress: Progress): Mastery {
  const m: Mastery = { new: 0, learning: 0, review: 0, total: words.length }
  for (const w of words) {
    const st = progress.words[w.id]?.state
    if (st === 'review') m.review++
    else if (st === 'learning') m.learning++
    else m.new++
  }
  return m
}

/** 累计量。平均只按「有复习的天」算 —— 把没打开 app 的日子算进分母会低估强度。 */
export function cumulativeTotals(progress: Progress): Totals {
  const days = Object.values(progress.dailyStats)
  const active = days.filter(d => d.reviewed > 0)
  const totalReviewed = days.reduce((n, d) => n + d.reviewed, 0)
  const totalNew = active.reduce((n, d) => n + d.newLearned, 0)
  return {
    totalReviewed,
    activeDays: active.length,
    avgNewPerActiveDay: active.length === 0 ? 0 : totalNew / active.length,
  }
}
```

**Note** that `addDays(today, -i)` goes through the existing implementation in `src/lib/srs.ts`, which already handles crossing month boundaries and local time zones — don't hand-roll date arithmetic here.

- [ ] **Step 4: Run and confirm it passes** → `npx vitest run src/pages/statsDerive.test.ts` all PASS

- [ ] **Step 5: Commit**

```bash
git add src/pages/statsDerive.ts src/pages/statsDerive.test.ts
git commit -m "feat: 学习数据派生纯函数"
```

### Task 8: "Recent" block on the Today page

**Files:**
- Modify: `src/pages/Today.tsx`, `src/pages/Today.css`

- [ ] **Step 1: Render the last 7 days**

Append a `Card` after the Today page's existing content: titled "Recent," containing a bar chart of the last 7 days' review volume (using `dailySeries(progress, today, 7)`) and the average accuracy over those 7 days; the whole block is a link to `/stats`.

The bar chart is **hand-written CSS**, no charting library: one `div` per day, height as a percentage of `reviewed / max`; when `max` is 0, render all bars at the shortest tier rather than 0 height (otherwise the whole block looks broken).

The derivation calls must be wrapped in `useMemo(..., [progress, today])` — `useApp()`'s context value is a new object on every provider render, and the background sync heartbeat would cause pointless recomputation otherwise.

- [ ] **Step 2: Styling** — append to `Today.css`, class prefix `.today-recent`. Bars use `--accent` (progress-type elements are an explicitly permitted use of vermillion per tokens.css).

- [ ] **Step 3: Verify and commit**

Run: `npx tsc -b --noEmit && npm test && npm run build && npx oxlint` → all green

Manual contract: for a new user (no dailyStats) the block shows an empty state rather than blank space or `NaN`; no error with only one day of data; the 7 bars don't overflow at 375px.

```bash
git add src/pages/Today.tsx src/pages/Today.css
git commit -m "feat: 今日页显示最近 7 天学习情况"
```

### Task 9: /stats page

**Files:**
- Create: `src/pages/Stats.tsx`, `src/pages/Stats.css`
- Modify: `src/App.tsx` (only adding one route)

- [ ] **Step 1: Add the route**

Append this to the existing protected routes in `src/App.tsx` (at the same level as `/word/:id`):

```tsx
          <Route path="/stats" element={<Stats />} />
```

**Don't add it to `TabBar`** — the four bottom slots (Today/Library/Test/Settings) are already full, and squeezing in a fifth would hurt the mobile feel; the entry point is the "Recent" block on the Today page.

- [ ] **Step 2: Page contents**

`Page`'s `back="/"`, presenting in order:

1. Bar chart of daily review volume for the last 30 days (`dailySeries(progress, today, 30)`)
2. Accuracy trend (`accuracySeries(progress, today, 30)`, days with `null` are broken in the line rather than drawn at 0)
3. Streak length + a streak-break calendar: one small square per day for the last 30 days, filled in on days with a review. Streak length reuses `computeStreak` (already in `src/pages/todayStats.ts`, import it directly — it's part of the same file group)
4. Word-list mastery distribution (`masteryBreakdown`): a stacked bar + three numbers, reusing the `.stat` primitive
5. Cumulative (`cumulativeTotals`): total review count, mastered-word count, average daily new words, reusing the `.stat` primitive

All derivation calls wrapped in one `useMemo(..., [words, progress, today])`.

- [ ] **Step 3: Empty state**

When there's no `dailyStats` at all, the whole page shows `.empty-state` (an existing shared primitive): "No study history yet — this will fill in once you've reviewed a few words." + a button back to Today. **Don't render a page full of empty charts.**

- [ ] **Step 4: Verify and commit**

Run: `npx tsc -b --noEmit && npm test && npm run build && npx oxlint` → all green

Manual contract: `#/stats` is reachable and can be entered from the Today page; empty data goes to the empty state; single-day data doesn't error; no horizontal overflow at 375px; readable in both light and dark themes; values match a manual calculation against `localStorage['volcab.progress']`.

```bash
git add src/pages/Stats.tsx src/pages/Stats.css src/App.tsx
git commit -m "feat: 统计页"
```

---

## Wrap-up

Once all three groups are merged into `master`:

- [ ] `npm test && npx tsc -b --noEmit && npm run build && npx oxlint && npm run validate-words` all green
- [ ] Push `master`, `gh run watch` until deployment is green
- [ ] Spot-check in production: `#/stats` reachable, quiz can generate the new question types, the review page shows "N left"
- [ ] Update the status and known issues in `docs/superpowers/HANDOFF.md`

## Self-Review Notes

- **Spec coverage**: §2.1 → Task 1; §2.2 → Task 2; §7.3 the three question types → Task 3/4/5; §7.4 rotation → Task 5 Step 4; §7.5 modifying the existing assertion → Task 5 Step 1; §5.2 placement → Task 8/9; §5.3 metrics list → Task 7/9. No gaps.
- **Type consistency**: `remaining(q)`, `clozeExample(sentence, headword)`, `clozeCollocation(collocation, headword)`, `sharedSynonyms(words)`, `QUIZ_TYPES`, `dailySeries/accuracySeries/masteryBreakdown/cumulativeTotals` have consistent signatures between the tests and the implementation; `QuizQuestion.hintKind` is only carried by `synonymHint`, the same pattern as `phonetic` only being carried by `spelling`.
- **Placeholders**: none. UI tasks (Task 2 Step 3, Task 6, Task 8, Task 9) are described via behavior contracts rather than line-by-line JSX — this follows the same decision established in the v1.0 plan (logic is already tested at the pure-function layer), not an omission.
